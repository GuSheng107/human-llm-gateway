# Human LLM Gateway 开发规范

本文件是本仓库所有人工开发者与编码代理必须遵守的最高优先级项目规范。产品边界、架构和进度文档后续统一放在 `docs/`，不得把 IDE 私有目录当作项目规范来源。

## 1. 产品边界

- 本项目是“由人类或用户自选真实 LLM 生成内容，再伪装成真实 LLM API 返回”的网关。
- API Key 决定请求归属用户、回复入口、回复策略和对外可用 Fake Model；Fake Model 只是对外身份，不映射真实上游模型。
- 管理员维护全局系统 Fake Model，普通用户可以创建仅自己可见的私有 Fake Model。模型分组先预筛用户可见模型，API Key 可继续选择具体模型；Key 不选择模型代表允许全部候选模型。
- 用户可选择一个自己的 IM 连接接收任务，或直接在 Web 回复；无论选择哪种入口，任务始终在 Web 可见。
- 每个用户最多同时存在 10 个活动任务，所有 API Key、人工模式、真实 LLM 模式和 fallback 共用该限制；超限直接返回协议兼容的 429。
- 支持的外部推理协议仅限 OpenAI Chat Completions、OpenAI Responses、Anthropic Messages。
- 人工回复必须先提交完整结果，再进行伪流式输出。假 tool call 不在本系统执行，也不等待执行结果。
- IM DSL 与 Web 回复编辑器必须生成同一个规范化回复结构；提交前允许预览和编辑，首个有效提交成功后不可撤销或覆盖。
- 用户可维护自己的 LLM 配置。手动调用 LLM 时结果先成为可编辑草稿；仅 LLM 和人工超时 fallback 模式可以自动返回上游结果。
- 转发同协议请求时保留调用方的全部原始字段，并追加基于 Fake Model 的身份 system 指令；响应中的模型标识必须改写为请求的 Fake Model。
- `previous_response_id` 等明确列入契约的网关控制字段由网关校验、关联历史并等价展开；原始请求仍完整保存，字段消费或重写不得静默发生。
- 跨协议转发无法等价转换专有字段时，明确返回协议兼容的 400，不得静默丢弃。
- 上游返回的 tool call 可以转发给外部调用方，但本系统不执行。未来真实工具只允许在隔离沙箱和管理员白名单内运行。
- 只有管理员可以签发邀请码。邀请码支持过期时间、撤销和使用次数限制。
- 管理员不能替用户回复任务，不能查看用户的密码、完整 API Key、LLM Secret、IM Token 或登录二维码。
- 管理员禁用用户时必须立即撤销其会话和 API Key、终止活动任务并幂等释放名额；对外只返回通用协议错误。
- 不兼容旧数据库、旧 API 或旧连接器。当前阶段直接使用新表结构创建数据库，不写兼容分支或迁移兜底。
- M2 必须以一次完整提交原子切换到目标结构；不得把新旧表、新旧运行链路、双写或兼容代理同时提交到 `master`。

## 2. Git 与变更边界

- 本项目只使用 `master` 分支，不创建其他分支。
- 修改前先检查 `git status`、当前分支和远端；保留用户已有且与任务无关的改动。
- 每个里程碑最终提交必须独立、可运行、可验证；提交信息使用 Conventional Commits 前缀和中文说明。
- M2 为原子重构特例：本地未提交工作区在实施期间允许处于不可运行状态（测试失败、前端构建失败），但不得把该中间状态提交或推送到 `master`。M2 内部工作包只用于跟踪进度，不能作为新旧结构共存的中间提交；只有目标结构、测试和前端一起切换完成且通过完整质量门禁后才形成一个提交。master 历史中只出现切换前和完整切换后两个可运行状态。
- 只暂存当前任务文件。禁止提交数据库、日志、构建缓存、`.env`、明文凭据、演示密钥和 IDE 私有目录。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令覆盖用户改动。
- 推送前必须确认本地 `master` 与 `origin/master` 的关系，并完成质量门禁。

## 3. Python 后端规范

- 使用 Python 3.12，依赖统一写入 `pyproject.toml`，由 `uv.lock` 锁定；禁止绕过 `uv` 临时安装未声明依赖。
- 目录职责固定：
  - `app/api/`：FastAPI Router、请求解析、响应和鉴权依赖。
  - `app/domain/`：领域枚举、状态机、策略和不依赖基础设施的业务规则。
  - `app/services/`：用例编排与事务边界。
  - `app/repositories/`：SQLAlchemy 查询与持久化。
  - `app/connectors/`：IM 连接器和连接器注册表。
  - `app/protocols/`：三种外部 LLM 协议的解析、错误和输出适配。
  - `app/core/`：配置、数据库、安全、异常和日志基础设施。
- 依赖方向为 `API -> Service -> Repository/Domain`。Router 不得堆积 SQL、连接器细节或复杂业务规则。
- 不新增巨型单文件 `api.py`、`services.py` 或职责不明的 `utils.py`。模块应按领域命名。
- 请求和响应使用明确的 Pydantic 模型；稳定业务值使用集中定义的枚举或常量，环境相关值使用配置。
- 所有公开函数和跨模块数据结构必须有类型标注。异步 I/O 不得在事件循环中调用阻塞操作。
- 原始 LLM 请求完整保存；用于展示或跨协议转换的规范化数据必须单独生成，不能覆盖原始请求。
- 管理 API 使用统一错误结构；`/v1/*` 使用对应 OpenAI 或 Anthropic 错误格式。对外错误不得泄露人工、IM、fallback、内部路径或异常堆栈。
- 日志结构化并尽量携带 `request_id`、`task_id`、`user_id`、`api_key_id`、`connector_id`。禁止记录任何完整 Secret。
- 数据库操作必须明确所有权过滤和事务边界。并发名额、邀请码消费、首次回复等竞争操作必须原子化。
- 测试按领域组织，至少覆盖正常路径、权限隔离、状态竞争、协议契约和失败恢复。

## 4. 前端规范

- 固定使用 React 19、TypeScript 严格模式、Vite、React Router 和 Tailwind CSS 4。
- 不引入 Vue、Element Plus、CSS-in-JS 或另一套全量 UI 框架。RuoYi 仅作为后台信息架构、布局和交互密度参考。
- 目录按职责拆分为 `api/`、`components/`、`features/`、`types/`；业务页面放在对应 feature 中，通用组件不得依赖具体业务。
- 页面统一采用浅色 RuoYi 风格：侧栏、顶栏、面包屑或页面标题、筛选区、紧凑表格、分页和抽屉详情；避免大面积黑色主体。
- 页面结构优先为“标题/说明 -> 操作区 -> 筛选区 -> 数据区 -> 分页”。短操作使用弹窗，复杂详情和编辑使用抽屉。
- 菜单、路由、标题、权限和说明文案使用单一配置来源，不得在多个组件重复维护。
- Tailwind 重复颜色、尺寸和阴影应抽取为 CSS 变量、主题 Token 或通用组件；不得在页面散落重复任意值。
- API 调用按领域拆分并统一处理鉴权、错误与 401。TypeScript 类型与后端响应契约同步维护。
- 所有关键页面验证 1440px、1024px、390px；弹窗支持 Esc，交互元素支持键盘访问，不依赖悬停才能操作。
- 密码、API Key 和 Secret 仅在必要的一次性场景显示，离开页面后不能从前端状态重新取回。

## 5. API 与安全规范

- 管理台接口统一使用 `/api/*`；推理接口使用 `/v1/*`；连接器入口使用 `/connectors/*`；健康检查使用 `/healthz`。
- 新接口使用正确的 HTTP 方法和稳定的错误码。除非产品文档明确要求，不增加旧路径兼容。
- `/v1/models` 按“用户可见模型 -> 可选模型分组 -> Key 可选模型”计算并只返回当前 API Key 可用的有效 Fake Model；Key 未选择模型时返回候选集全部模型，不暴露真实 LLM 模型。
- `/v1/models` 必须使用 API Key 鉴权，不提供匿名模型目录；SDK 必须先配置 Key 再发现模型。
- 请求中的 Fake Model 必须验证；不存在或已停用时返回协议兼容的 `model_not_found`。
- 同协议默认完整透传未知字段；网关控制字段必须在契约中逐项声明。跨协议转换必须遵循字段矩阵，只能透传、等价转换、由网关消费或明确返回 400。
- 管理员可监管用户资源，但任何读取或操作能力都必须显式授权并记录审计。
- 页面上下文交给 Web 小助手前必须过滤密码、完整 Key、Token、Secret 和连接凭据。
- 禁止执行调用方声明的工具。未来系统工具必须同时满足管理员白名单、用户权限、显式确认和沙箱限制。

## 6. 质量门禁

提交和推送前必须全部通过：

```text
uv lock --check
uv run --locked ruff format --check app tests
uv run --locked ruff check app tests
uv run --locked python -m pytest -q
npm ci                         # workdir: admin
npm run build                  # workdir: admin
git diff --check
```

任何失败都必须修复或在交付中明确说明，不能把“构建成功”替代测试、格式、权限和协议验证。

## 7. 完成定义

一个里程碑只有在以下条件全部满足时才能标记完成：

- 产品行为和权限边界实现。
- 后端、前端和数据库契约一致。
- 自动化测试覆盖核心验收项并通过。
- 用户可见页面不是占位内容。
- 相关文档和 `docs/ROADMAP.md` 状态同步更新。
- 工作区无本任务产生的敏感文件或无关生成物。
- 代码已提交并推送到 `origin/master`。
